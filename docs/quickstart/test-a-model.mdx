---
sidebar_position: 3
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Make predictions

To test a model you first need to have an active model. Make sure you have the correct **modelId** to the model you want to test.

<Tabs groupId="programming-language">
<TabItem value="cli" label="CLI" default>

```bash
las models get <modelId> # Make sure that the model is active
las documents create receipt.pdf
las predictions create <documentId> <modelId> # use the documentId from the previous step
```

</TabItem>
<TabItem value="curl" label="cURL">

```bash
curl -X POST 'https://api.lucidtech.ai/v1/documents' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer eyJra...' \
--data-raw '{
    "content": "JVBERi0xLjQ...",
    "contentType": "application/pdf",
}'

curl -X POST 'https://api.lucidtech.ai/v1/predictions' \
--header 'Content-Type: application/json' \
--data-raw '{
    "documentId": <documentId>,
    "modelId": <modelId>
}'
```

</TabItem>
<TabItem value="python" label="Python">

```python
client.get_model('<modelId>') # Make sure that the model is active
document = client.create_document(b'<bytes data>', 'application/pdf')
client.create_prediction(document_id=document['documentId'], model_id='<modelId>')
```

</TabItem>
<TabItem value="app" label="App">

* Go to the **models** section
* Select the model you want to test, make sure that the status of the model is **active**.
* Click on the tab named **Test**
* Choose **Upload from device** and select a document you want to process

</TabItem>
</Tabs>


If you want to learn more about the basics, check out [models](../concepts/models) and [predictions](../concepts/predictions) in the Concepts section

If you want to see more examples of how you can use your model to process your documents, check out the [Process documents](./serve-a-model) page